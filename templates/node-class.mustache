/*jshint -W069 */
'use strict';

/**
* A promise object provided by the q promise library.
* @external Promise
* @see {@link https://github.com/kriskowal/q/wiki/API-Reference}
*/

/**
 * {{&description}}
 * @class {{&className}}
 * @param {string} domain - The project domain
 * @param {integer} timeout - request timeout if not set defaults to 10 seconds
 */
var {{&className}} = (function(){

    var request = require('request');
    var Q = require('q');

    // default timeout is 10 seconds i.e. 10000 milliseconds
    var defaultTimeout = 10000;

    function {{&className}}(domain,timeout){
        this.domain = domain;
        this.timeout = timeout!==undefined?timeout:defaultTimeout;
    }

    {{#methods}}
        {{> method}}
    {{/methods}}

    return {{&className}};
})();

exports.{{&className}} = {{&className}};
