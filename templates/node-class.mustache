/*jshint -W069 */
/**
 * {{&description}}
 * @class {{&className}}
 * @param {string} domain - The project domain
 */
var {{&className}} = (function(){
    'use strict';

    var request = require('request');
    var Q = require('q');

    function {{&className}}(domain){
        this.domain = domain;
    }

    {{#isSecurity}}
        var token;
        {{&className}}.prototype.setToken = function(_token){
            token = _token;
        };
    {{/isSecurity}}

    {{#methods}}
        {{> method}}
    {{/methods}}

    return {{&className}};
})();

exports.{{&className}} = {{&className}};
