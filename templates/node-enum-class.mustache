/*jshint -W069 */
'use strict';

(function(enum) {
	
/**
 * {{className}} {{description}}
 * @readonly
 * @enum { {{enumValueType}} } 
 * @swagger enum [{{#enums}} "{{enumValue}}" {{^last}}, {{/last}} {{/enums}}]
 */
enum.{{className}} = {
		{{#enums}}
			{{enumType}}:'{{enumValue}}' {{^last}}, {{/last}}
		{{/enums}}
	};

 })(exports.module);


